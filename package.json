{
  "name": "servers-availablity",
  "version": "1.0.0",
  "description": "A node module that determines the availability of a given list of servers and then returns an available server with the lowest priority number. ",
  "main": "index.js",
  "scripts": {
    "test": "jest --watch",
    "start": "NODE_ENV=development node demo.js",
    "watch": "NODE_ENV=development nodemon demo.js"
  },
  "author": "Sherif Hussien",
  "license": "ISC",
  "devDependencies": {
    "jest": "^24.9.0",
    "nodemon": "^1.19.3"
  },
  "dependencies": {
    "axios": "^0.21.1"
  }
}
